name: offramp
description: Your evening, reclaimed.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  hive_flutter: ^1.1.0
  hive: ^2.2.3
  provider: ^6.1.1
  speech_to_text: ^7.0.0
  permission_handler: ^11.3.0
  flutter_local_notifications: ^17.0.0
  home_widget: ^0.9.0
  flutter_foreground_task: ^6.1.2
  audioplayers: ^5.2.1
  flutter_animate: ^4.5.0
  intl: ^0.19.0
  shared_preferences: ^2.2.2
  workmanager: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.7

flutter:
  uses-material-design: true
